# Copy this to .env and fill in your values
ANTHROPIC_API_KEY=sk-ant-...

# SQLite path (local only — do NOT commit corpus.db)
CORPUS_DB_PATH=./data/corpus.db

# For deploy: Cloudflare D1 binding name (set in wrangler.toml)
# CF_D1_BINDING=corpus_db
